Small uppercase status pill — schedule state, shift status.

```jsx
<Badge tone="success">Published</Badge>
```

Tones: `success`, `warning`, `danger`, `info`, `neutral`. Always sentence
content in caps via the component (don't manually uppercase children).
