Domain primitive (intentional addition) — initials avatar (no photo assets
exist) with a status dot for availability/clock-in state.

```jsx
<AvatarStatus name="Maria Garcia" status="available" />
```

Statuses: `available` (green), `unavailable` (red), `pending` (amber —
awaiting time-off approval), `off` (neutral gray).
