General-purpose surface — wraps shift summaries, settings sections, list rows.

```jsx
<Card hoverable><ShiftBlock ... /></Card>
```

Set `hoverable` to raise elevation from `shadow-sm` to `shadow-md` on hover
(used for clickable cards, not static ones).
