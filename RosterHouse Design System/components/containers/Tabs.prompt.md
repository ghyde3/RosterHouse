Segmented tab control for switching views within a screen.

```jsx
<Tabs tabs={[{value:'sched',label:'Schedule'},{value:'avail',label:'Availability'},{value:'time-off',label:'Time off'}]} onChange={setView} />
```
