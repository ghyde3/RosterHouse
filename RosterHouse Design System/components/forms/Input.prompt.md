Labeled text input for forms — availability notes, shift titles, employee search.

```jsx
<Input label="Shift title" placeholder="e.g. Line cook" value={value} onChange={handleChange} />
```

Pass `error` to show a validation message and switch the border to danger
red. Pass `icon` (a Lucide icon element) for a leading glyph, e.g. a search
field.
